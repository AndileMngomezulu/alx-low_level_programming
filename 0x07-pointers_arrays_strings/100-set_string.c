#include "main.h"

/**
  * set_string - it is the main function
  * @s: pointer to pointer
  * @to: pointer to char
  * Return: always 0 if successful
  */
void set_string(char **s, char *to)
{
	*s = to;
}
