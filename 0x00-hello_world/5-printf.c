#include <stdio.h>

/**
 * main - the main function that prints exactly
 *
 * Return: 0 when it succeed
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
